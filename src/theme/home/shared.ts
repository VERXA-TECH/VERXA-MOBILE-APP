/** Home shell — top glow asset sizing from Figma export (430×482). */
export const homeShared = {
  topGlow: {
    /** Native asset dimensions — used to preserve aspect ratio on any device width. */
    assetWidth: 430,
    assetHeight: 482,
    widthRatio: 1,
    offsetTop: 0,
  },
} as const;
